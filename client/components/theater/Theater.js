import React, { Component } from 'react';
import { connect } from 'react-redux';

class Theater extends Component {
  render() {
    return(
      <div></div>
    )
  }
}

export default connect()(Theater);
